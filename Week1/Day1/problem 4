#include <stdio.h>

int funn(int a, int b) {
    if (a && b && (a + b > 0)) {
        return a + funn(a - 2, b - 2) + b;
    }
    return a ^ b;  // Bitwise XOR
}

int main() {
    int a = 8, b = 8;
    int result = funn(a, b);
    printf("%d\n", result);
    return 0;
}
  output : 40
