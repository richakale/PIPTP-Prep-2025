#include <stdio.h>

void fun(int w, int x) {
    int y = 0;

    if ((x % w == 0) || (w % x == 0)) {
        y = y + 1;
    } else {
        y = y + 10;
    }

    printf("%d\n", y);
}

int main() {
    int w = 40, x = 4;
    fun(w, x);
    return 0;
}

output: 1
